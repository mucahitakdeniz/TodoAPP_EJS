<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TODO READ</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>

  <body class="bg-success-subtle">
    <br />
    <br />
    <div class="container">
      <div class="row">
        <h1 class="text-center text-secondary m-3">TODO READ</h1>
        
        <p class="text-end mb-5">
          <a class="btn btn-sm btn-secondary" href="/view">List Todos</a>
          <a class="btn btn-sm btn-primary" href="/view/create">New Todo</a>
        </p>
        <table class="table table-striped">
          <thead>
            <% let color= todo.isDone ? 'text-success' : 'text-dark' %>
            <tr>
              <th>ID</th>
              <th class="<%= color %>"># <%= todo.id %></th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Title</td>
              <td class="<%= color %>"><%= todo.title %></td>
            </tr>
            <tr>
              <td>Description</td>
              <td class="<%= color %>"><%= todo.description %></td>
            </tr>
            <tr>
              <td>Priority</td>
              <td class="<%= color %>"><%= PRIORITY[todo.priority] %></td>
            </tr>
            <tr>
              <td>İs Done?</td>
              <td class="<%= color %>"><%= (todo.isDone ? 'Yes' : 'No') %></td>
            </tr>
            <tr>
              <td>Created</td>
              <td class="<%= color %>">
                <%= todo.createdAt.toLocaleDateString('tr') %>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
